info:
  name: Get Single Tea
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/teas/31"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
