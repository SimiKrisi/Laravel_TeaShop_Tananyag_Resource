info:
  name: Delete Tea
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/teas/26"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
