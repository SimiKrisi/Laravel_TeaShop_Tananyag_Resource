info:
  name: Update Tea
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/teas/1"
  body:
    type: json
    data: |-
      { 
        "stock" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
